import { getLib } from '../global-api/'

export const Cesium = getLib('Cesium')
export const turf = getLib('turf')
export const echarts = getLib('echarts')
export const Supercluster = getLib('Supercluster')
